StringBuffer sbf = new StringBuffer();


常用方法

append(字符串)					追加字符串
insert(插入位置，字符串)			插入字符串
replace(替换开始位置，替换结束位置，字符串)	替换字符串
indexOf()					过滤字符串中存在的字符

==========================================================================

StringBuilder